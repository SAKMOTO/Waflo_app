fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.2
# browser-use is VENDORED inside the repo (<repo>/browser-use, v0.13.8).
# Install it from there so the exact checked-in copy is used — the old
# PyPI pin (browser-use==0.1.48) had a different API and silently disabled
# the commerce agent. The server also boots it via vendor_browser_use.py.
-e ../browser-use
# Playwright is required at runtime by the vendored browser-use (ARIA/commerce
# browser agents). Must be pip-installed here AND its browser downloaded with:
#   python -m playwright install chromium
# (It was previously missing from requirements, so recreated venvs could not
# start any browser agent.)
playwright
# Firecrawl is VENDORED inside the repo as a fully self-hosted service
# (<repo>/firecrawl, incl. apps/python-sdk, firecrawl-py v4.41.0). The ARIA
# research adapter imports the SDK directly from the vendored folder via
# vendor_firecrawl.py, so NO pip install is required here. Install the SDK
# as an editable copy only if you prefer regular installs:
# -e ../firecrawl/apps/python-sdk
# Its runtime deps (requests, httpx, python-dotenv, websockets, pydantic>=2,
# aiohttp) are already provided by the pins above.
huggingface-hub==0.31.2
tavily-python==0.5.0
trafilatura==2.0.0
websockets==15.0.1
numpy
sentence-transformers
openai
langchain-google-genai